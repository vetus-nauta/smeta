# Проверка перед переносом в GitHub

09.09.2026. Пройдены 15 тестов: арифметика, локальный реестр, сохранение утверждённых байтов XML, отказ при изменённом хеше или отсутствии утверждения, защита существующей версии, запрет DTD в UTF-16, приложения заказчика. Команда: `python3 -m unittest discover -s checks -p "test_*.py" -v`.

Четыре навыка прошли quick_validate. SQLite остаётся локальной производной базой. XSD конкретной сметы и производственные XML не проверялись: их ещё нет. Исходная приёмка v1 с 9 тестами сохранена как исторический этап; последующий набор расширен до 15.
